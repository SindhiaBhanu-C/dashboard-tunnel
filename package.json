{
  "name": "railway-ssh-dashboard-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Railway-hosted reverse proxy that reaches a private dashboard through an SSH tunnel.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  }
}
